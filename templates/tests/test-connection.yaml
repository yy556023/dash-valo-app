apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dash-valo-app.fullname" . }}-test-connection"
  labels:
    {{- include "dash-valo-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dash-valo-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
