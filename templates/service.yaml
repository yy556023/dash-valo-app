apiVersion: v1
kind: Service
metadata:
  name: {{ include "dash-valo-app.fullname" . }}
  labels:
    {{- include "dash-valo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "dash-valo-app.selectorLabels" . | nindent 4 }}
